name: retro_c64
description: "Retro-C64 -- a Commodore 64 emulator and workbench for Linux, Android and iOS."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# Build 1 was delivered to App Store Connect from this project and build 2 was
# prepared on the ios-beta-prep branch, so both numbers are treated as spent:
# App Store Connect refuses a CFBundleVersion it has already seen, and it says
# so after the upload rather than before it. Xcode Cloud takes CFBundleVersion
# from here via ios/ci_scripts/ci_post_clone.sh, so the bump has to live here.
version: 1.0.0+41

environment:
  sdk: ^3.13.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  ffi: ^2.1.3
  path: ^1.9.0
  file_picker: ^12.0.0
  shared_preferences: ^2.3.5
  path_provider: ^2.1.5
  gamepads: ^0.1.10+5
  http: ^1.6.0
  archive: ^4.0.9
  provider: ^6.1.1
  get_it: ^8.0.2
  package_info_plus: ^10.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/retro_recomp_logo.png
    # BUNDLED ON PURPOSE: the OPEN ROMs, which are not Commodore's.
    #
    # assets/vice/OPENROMS holds the MEGA65 team's Open ROMs -- a clean-room
    # BASIC, KERNAL and character set under the LESSER GPL v3, not
    # Commodore's code. Both licence texts ship: the LGPL is written as an
    # additional permission on top of the GPL v3, so COPYING alone states
    # the licence wrongly. They
    # ship so the app can show a WORKING C64 the moment it is installed, with
    # nothing for the user to supply and nobody's copyright infringed.
    #
    # They do not replace the real ROMs: Open ROMs is an ongoing project and
    # does not implement every KERNAL routine, so software written against the
    # originals may not run on them. That is what the ROM import is for, and
    # why the wizard says so in as many words rather than letting someone
    # discover it when a game fails.
    # The demo the user picks out of their games list to see the emulator
    # work. Ours, generated by tool/make_demo_prg.py -- machine code behind a
    # SYS stub, so it depends on no BASIC and runs on either ROM set.
    - assets/demo/demo.prg
    - assets/vice/OPENROMS/kernal
    - assets/vice/OPENROMS/basic
    - assets/vice/OPENROMS/chargen
    - assets/vice/OPENROMS/README.txt
    - assets/vice/OPENROMS/COPYING
    - assets/vice/OPENROMS/COPYING.LESSER
    - assets/vice/OPENROMS/LICENSE.txt
    # DELIBERATELY NOT BUNDLED: the C64 ROMs.
    #
    # assets/vice/C64 and assets/vice/DRIVES hold Commodore's kernal, BASIC,
    # chargen and 1541 DOS ROMs. Those are still under copyright (Cloanto
    # today), and shipping them in a store build is not ours to do. The app
    # asks the user to supply them instead -- see RomStatus and the guidance
    # on the Paths screen.
    #
    # SID tunes for the Music tab. These are commercial game compositions
    # whose composers still hold the rights; bundling them is a deliberate
    # product decision, not an oversight. The Music tab ALSO plays whatever
    # the user imports, so this is a starting playlist rather than the only
    # source.
    - assets/sids/
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
