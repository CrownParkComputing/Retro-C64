// Placeholder smoke test. The default counter-app template test was
// replaced along with main.dart's content; real widget tests for the
// workbench (sidebar navigation, library grid column math, FFI binding
// wiring) are deferred to a later milestone.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
